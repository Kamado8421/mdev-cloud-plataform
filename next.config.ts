import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    // Outras flags experimentais aqui (se precisar)
  },
  allowedDevOrigins: ["*"],
};

export default nextConfig;
